# OBS Auto Scene Switcher - Universal Requirements
# Install with:
#   python -m pip install -r requirements.txt
#
# Platform-specific packages are handled automatically.

# All platforms
python-dotenv>=1.0.0

# Windows only (OBS_Monitor_Server_Windows.py)
pywin32>=306; sys_platform == "win32"
psutil>=5.9.0; sys_platform == "win32"

# Mac only (OBS_Scene_Switcher_Client.py)
obsws-python>=1.6.0; sys_platform == "darwin"